import java.util.HashMap;

public class CarrinhoDeCompras {
	
	Pizza p = new Pizza("queijo", 10);
	
	
	public double valorTotal(Pizza p) {
		//não aceitar p.qtdItens = 0
		//fazer um loop no hashmap, listando item, qtd, preço 
		//calculando preco*qtde 
		//acululando no total 
		// e ao final totalizar o pedido
		//System.out.println("mapa no carrinho"+map);
		return 0;
	}
	
	/*
	public void adicionaPizza(Pizza p, int qtde)
	{				
	    if (!map.containsKey(p.chave)) {
	        System.out.println("Não Tem!");
	    }				

	    contador = carrinho.get(prod) + 1;
	    carrinho.put(p, contador);
	    System.out.println(contador);	
	    System.out.println("Tem!");
	}
	*/
}
